<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Reporte</title>
</head>
<body class="m-0 py-0 px-3">
    <section class="d-flex justify-content-between py-1 px-5">
        <span class="w-50">
            <p class="m-0">Nombre: {{user.firstName}} {{user.lastName}}</p>
            <p class="m-0">Correo: {{user.email}}</p>
            <p class="m-0">Fecha de emisión: {{date}}</p>
            <p class="m-0">Canal de ventas: Mercado Libre</p>
        </span>
        <span class="d-flex align-items-center">
            <img style="width: 5rem;" src="https://http2.mlstatic.com/storage/platsec-applications-dev-mngr/prod-applications-fe/1696312661505_1489297309.png" alt="ML LOGO">
        </span>
    </section>
    <table class="table table-striped">
        <thead>
          <tr>
            <th scope="col">Imágen</th>
            <th scope="col">Fecha</th>
            <th scope="col">Nombre</th>
            <th scope="col">Precio</th>
            <th scope="col">Comisión</th>
            <th scope="col">Cantidad</th>
          </tr>
        </thead>
        <tbody>
          {{#each items}}
            <tr>
              <td class="flex justify-content-center mx-auto w-25">
                  <img class="rounded border border-2 w-50" src="{{item.image}}" alt="producto">
              </td>
              <td>{{created}}</td>
              <td>{{item.title}} ({{item.id}})</td>
              <td class='text-nowrap'>${{unit_price}} MXN</td>
              <td class='text-nowrap'>${{sale_fee}} MXN</td>
              <td class='text-center'>{{quantity}}</td>
            </tr>
          {{/each}}
        </tbody>
    </table>
    <div class="d-flex justify-content-end px-5">
        <div class="d-flex justify-content-between gap-3">
          <div class="d-flex flex-column">
            <span>Subtotal:</span>
            <span>Comisiones:</span>
            <span>Total:</span>
          </div>
          <div class="ml-2 d-flex flex-column">
            <span class="fw-bold text-left">${{totalEarnings}} MXN</span>
            <span class="fw-bold">${{totalFees}} MXN</span>
            <span class="fw-bold">${{total}} MXN</span>
          </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>